/** @type {import('next').NextConfig} */

//const { getUrls } = require('./utils/index.ts');

const nextConfig = {
  reactStrictMode: true,
  // async redirects() {
  //   return await getUrls();
  // },
};

module.exports = nextConfig;
